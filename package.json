{
    "scripts": {
      "install-all": "npm --prefix backend install && npm --prefix frontend install",
      "start-all": "npm --prefix backend run start && npm --prefix frontend run start"
    }
  }
  